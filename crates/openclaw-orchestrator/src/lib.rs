// OpenClaw Orchestrator — pure domain + app library
// No HTTP types. No Axum. No framework dependencies.

#[cfg(test)]
mod tests {
    #[test]
    fn crate_compiles() {
        assert!(true);
    }
}
